name: Deploy Both Versions to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build Website v1.0
        working-directory: ./website
        run: |
          npm install
          npm run build

      - name: Build Website v2.0
        working-directory: ./website_2.0
        run: |
          npm install
          npm run build

      - name: Assemble Combined Site
        run: |
          mkdir -p combined_pages
          # Version 1.0 at root
          cp -r website/dist/* combined_pages/
          # Version 1.0 also at /v1/
          mkdir -p combined_pages/v1
          cp -r website/dist/* combined_pages/v1/
          # Version 2.0 at /v2/
          mkdir -p combined_pages/v2
          cp -r website_2.0/dist/* combined_pages/v2/
          # SPA 404 fallbacks
          cp website/dist/index.html combined_pages/404.html
          cp website_2.0/dist/index.html combined_pages/v2/404.html
          touch combined_pages/.nojekyll

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./combined_pages

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
